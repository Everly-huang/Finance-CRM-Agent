# 项目落地清单

## 0. 说明

- **与需求文档的关系**:《金融产品智能推荐系统需求文档.md》回答"做什么与为什么"（含现状与规划标注）；本清单回答"还差什么与怎么验收"。
- **与评审报告的关系**:本清单源自《需求文档评审报告.md》第 6 节草案，经用户确认后成文；问题溯源可查报告对应问题 ID。
- **优先级定义**:P0 = 阻塞本地 demo；P1 = demo 之后；P2 = 二期。
- **工作量口径**:XS < 半天 / S ≤ 1 天 / M ≤ 3 天 / L ≤ 1 周。
- **里程碑**:里程碑 1 = 本地 demo 完整可跑（双进程 + 算法全流程 + AI 解读 + 合规声明）。

---

## 1. 工程化基础（ENG）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| ENG-01 | 修复 db_path 硬编码（backend/database/db_connection.py:8）为仓库相对路径 `os.path.join(os.path.dirname(os.path.abspath(__file__)), 'financial_products.db')` | 8.3 | **P0（阻塞 demo）** | XS | 删 D 盘库文件后启动，仓库内 financial_products.db 被复用，数据与文档一致 | — |
| ENG-02 | 生成 requirements.txt（fastapi、uvicorn、streamlit、plotly、requests；LLM 调用用 requests 走 OpenAI 兼容端点，减依赖） | 5.1、8.3 | P0 | S | `pip install -r requirements.txt` 一次装齐，双进程可起 | — |
| ENG-03 | git init + .gitignore（排除 *.db、secrets.toml、API key、__pycache__） | 8.3 | P0（面试工程口径） | XS | `git status` 不含敏感文件与数据库文件 | — |
| ENG-04 | 手动冒烟脚本（启动 → 首访 GET /api/products/ 预热 → 双算法各 1 例） | 8.1 | P0 | XS | 脚本按序执行无报错，输出与 6.2/6.3 口径一致 | ENG-01 |
| ENG-05 | pytest 接口级自动化（httpx TestClient 覆盖 5 个端点 + 建表幂等） | 6.1-6.3、8.1 | P1 | S | 测试全绿；重复启动不重复播种 | ENG-01 |

## 2. 数据层（DAT）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| DAT-01 | 新增 agent_audit_log 表 + audit_logger 写入模块（Schema：调用时间/输入/输出/校验结果） | 5.4、8.2 | P1（随 Agent 层） | S | 每次 Agent 调用落一条审计记录，含校验结果字段 | — |
| DAT-02 | customer_inputs 指标留档决策（是否存 similarity 明细，现仅 min_distance 单值） | 6.2 | P1 | XS | 决策记录在案并同步表结构 | — |

## 3. 算法层（ALG）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| ALG-01 | 算法输出补数值字段：KNN 输出 similarity/distance；Apriori 输出数值型 support/confidence/lift（保留现有字符串兼容） | 6.2、6.3 | P1（驱动匹配度可视化） | S | POST 响应含数值字段，可被 Plotly 直接绘图 | — |
| ALG-02 | 前端金额输入限制 30000-250000 + 裁剪提示（后端回传裁剪信息） | 6.1 | P1 | S | 输入 5000 时前端提示已裁剪至 30000 | — |
| ALG-03 | top_k/阈值参数化评估（含推荐权重 0.4/0.6 调节评估）：不默认开放给业务侧；如开放进"高级设置"需后端加参数+校验 | 2.3、5.5、6.3 | P2 | S | 评估结论文档化；若开放则 POST 传 k=3 恰返回 3 条 | — |
| ALG-04 | 合规约束过滤（风险等级适配性/起投金额门槛/产品可售状态校验——需先加"可售状态"字段） | 2.3 | P2 | M | 稳健客户不收到极高风险热门推荐（或以提示代替过滤） | — |

## 4. Agent 层（AGT）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| AGT-01 | 合规规则引擎 compliance_engine.py（关键词黑名单 + 正则拦截 + 输出后校验） | 5.4、6.4、8.2 | P0（demo 高潮段） | M | 喂入含"保本/稳赚"的 LLM 输出被拦截；无违规输出通过 | — |
| AGT-02 | llm_client.py（Deepseek OpenAI 兼容端点；timeout 30s/重试/模板降级） | 5.4、8.1 | P0 | S | 断网时返回"基础版解读"模板并标注，不抛异常 | — |
| AGT-03 | prompt_templates.py（System Prompt：合规边界 + 输出结构约束） | 5.4、6.4 | P0 | S | Prompt 注入"你是投资顾问"类攻击时 LLM 输出被合规引擎拦截 | — |
| AGT-04 | rag_retriever.py Mock 占位（返回空上下文） | 5.4 | P1 | XS | 接口可用，二期替换实现无需改调用方 | — |
| AGT-05 | 审计写入打通（接 DAT-01） | 5.4、8.2 | P1 | XS | 与 DAT-01 验收同 | DAT-01 |
| AGT-06 | RAG 二期：Chroma 向量库接入 + 产品文档表（数据层）+ 知识库构建 | 5.4、11 | P2 | M | 检索结果注入 Prompt 上下文，解读引用知识库内容 | AGT-04 |

## 5. 服务编排层（SVC）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| SVC-01 | recommendation_service.py（算法 → 合规引擎 → LLM → 审计流水线） | 5.1、5.4 | P0 | M | 一条调用链贯通推荐 + 解读 + 审计 | AGT-01/02/03 |
| SVC-02 | POST /api/recommend/explain 端点（推荐结果 + 用户上下文 → 合规解读，含降级模板路径） | 5.5 | P0 | S | curl 可拿到带免责声明的解读；断网时返回模板解读 | SVC-01 |
| SVC-03 | API 错误码规范（4xx + 统一错误结构，替代现状 200+error） | 5.5 | P1 | XS | 缺参返回 4xx + 结构化错误体 | — |
| SVC-04 | Pydantic 请求/响应模型引入 | 5.1、5.4 | P1 | S | /docs 展示请求 Schema；非法参数被 422 拦截 | SVC-03 |
| SVC-05 | DEMO_MODE 开关：True 时解读返回预置演示样例、禁止调用 LLM（环境变量控制）；默认 False 走 LLM；start_demo.py --demo 本地演练 | 8.3 | P1 | S | DEMO_MODE=True 时 explain 返回 source=demo 且不触及 llm_client；默认 False 行为不变 | — |

## 6. 交互层（UI）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| UI-01 | frontend/app.py（Streamlit：侧边参数配置/一键计算/结果表格/AI 解读面板/常驻免责声明，五区布局） | 5.5、6.1、9 | P0（demo 主界面） | L | 页面五区齐全；两个算法均可从页面跑通 | ENG-02 |
| UI-02 | CORS 白名单修正（backend/main.py:12 改允许 8501 或开发期放行） | 5.5 | P0（demo 前顺手） | XS | 浏览器直连 8000 调 API 不再被 CORS 拦截 | — |
| UI-03 | Plotly 可视化：风险等级分布/推荐类型占比先行；匹配度柱状图待 ALG-01 | 5.5 | P1 | S | 页面渲染至少 1 张 Plotly 图 | ALG-01 |
| UI-04 | 原型截图回填需求文档第 9 章 | 9 | P1（交互层完成后） | XS | 文档第 9 章附页面截图 | UI-01 |

## 7. 部署与演示（DEP）

| ID | 任务 | 关联文档章节 | 优先级 | 工作量 | 验收标准 | 依赖 |
|---|---|---|---|---|---|---|
| DEP-01 | 一键启动脚本 start_demo.bat/sh（自动修库路径依赖 → uvicorn 8000 → streamlit 8501 双进程） | 8.3 | P0 | S | 双击脚本后两个服务就绪，页面可访问 | ENG-01/02、UI-01 |
| DEP-02 | Deepseek key 注入文档化（环境变量 DEEPSEEK_API_KEY / streamlit secrets，不入 git） | 5.4 | P0 | XS | 无 key 时自动走降级路径；有 key 时正常调用 | AGT-02 |
| DEP-03 | 演示排练脚本（评审报告 5.3 节 6 组输入，含 70+250000 热门分流触发例） | 6.2、6.3 | P1 | XS | 按脚本走完 3-5 分钟演示全流程 | 里程碑 1 |
| DEP-04 | 公开部署路径评估（Streamlit Community Cloud 单进程 vs Render 双进程，含 key 管理与金融内容合规风险） | 8.3 | P2 | S | 输出部署方案决策文档 | — |

---

## 关键依赖链

```
ENG-01 → ENG-02/UI-01/UI-02/DEP-01
       → AGT-01/02/03 → SVC-01 → SVC-02   （AI 解读演示路径）
       → ALG-01 → UI-03 → UI-04           （可视化增强路径）
```

- **面试演示只需算法层的最小集**:ENG-01 + ENG-02 + UI-01 + UI-02 + DEP-01
- **完整 demo（含 AI 解读）**:最小集 + AGT-01/02/03 + SVC-01/02 + DAT-01/AGT-05

## 状态

| ID | 状态 | 完成日期 | 备注 |
|---|---|---|---|
| ENG-01 | ✅ 已完成 | 2026-09-03 | db_path 改为仓库相对路径（`os.path.join(os.path.dirname(os.path.abspath(__file__)), ...)`）；D 盘运行时副产物库已清理；双算法读写验证通过 |
| ENG-02 | ✅ 已完成 | 2026-09-03 | requirements.txt 已生成并安装至 finance-crm 环境（D:\Anaconda\envs\finance-crm，Python 3.11.16）；后端三端点 curl 验证通过；前端进程待 UI-01 建成后验证 |
| UI-02 | ✅ 已完成 | 2026-09-03 | CORS 白名单改为放行 Streamlit 8501（localhost + 127.0.0.1 双写法），移除遗留 5173；curl 预检与 GET 验证均回传 `access-control-allow-origin: http://localhost:8501`，5173 来源已被拒 |
| AGT-01 | ✅ 已完成 | 2026-09-03 | compliance_engine.py：12 个关键词黑名单 + 4 组违规正则 + 5 组注入特征；验收测试「保本/稳赚/保证收益」输出被拦截、干净输出通过。**2026-09-04 修正**：增加否定语境豁免（命中项所在句子含「不等于/不构成/严禁」等否定词组时放行），修复 LLM 合规教育话术（如「不等于无风险」「不构成收益保证」）被误拦导致解读整段降级；单元 7 用例全通过（3 豁免语境放行 + 3 真违规拦截 + 1 正常文本放行），端到端 source=llm、degraded=False；**2026-09-04 F1/F3**：validate_output 加 mode 参数——analysis 追加受众检测（检出「您」即漂移，审计记 audience_drift）、script 追加话术专项规则（SR-1 收益裸数字/SR-2 饥饿营销/SR-4 绝对化/算法指标词分区扫描），否定豁免复用；31 单元用例全通过；真实 LLM 采样复现两处备注区误伤并修复（否定词组补「请勿」、SR-1 收益裸数字改为只扫对客区），修复后 analysis 采样 24/24 零漂移、script 拦截率降至低频兜底 |
| AGT-02 | ✅ 已完成 | 2026-09-03 | llm_client.py：requests 直连 Deepseek OpenAI 兼容端点（eng02 减依赖口径）；timeout 8s + 重试 1 次；无 key/断网回退模板并标注"基础版解读"，验收通过不抛异常。**2026-09-04 修正**:timeout 8s→30s(真实解读实测生成约 18s,8s 误判 ReadTimeout 降级;前端 call_explain 同步 60s→90s),真实 key 端到端验证 source=llm |
| AGT-03 | ✅ 已完成 | 2026-09-03 | prompt_templates.py：System Prompt 含合规边界 5 条（禁投资建议/收益承诺/角色覆盖）+ 五段输出结构 + 结尾免责声明；「你是投资顾问」注入被输入校验拦截；**2026-09-04**：System Prompt 增【受众定位】段（读者=理财经理、第三人称、禁「您」、正反例）+ 语汇「用户→客户」+「业绩比较基准（非承诺）」口径；新增 SCRIPT_SYSTEM_PROMPT（双区五件套）+ 确定性话术 builder + 强制要素兜底；F6 待办：五段结构段 2（产品风险解读）与段 5（合规风险提示）合并优化（待复盘数据支撑） |
| AGT-04 | ✅ 已完成 | 2026-09-03 | rag_retriever.py Mock 占位（返回空上下文），接口签名与二期 Chroma 实现保持一致 |
| DAT-01 | ✅ 已完成 | 2026-09-03 | agent_audit_log 表（调用时间/输入/输出/校验结果 + 降级标记/模型）+ add_audit_log/get_audit_logs 数据访问函数，建表幂等 |
| AGT-05 | ✅ 已完成 | 2026-09-03 | audit_logger.log_agent_call 接入 SVC 流水线；验收测试离线 2 条（pass/blocked_input）+ HTTP 路径 1 条全部落库 |
| SVC-01 | ✅ 已完成 | 2026-09-03 | recommendation_service.py：推荐编排（KNN/Apriori，响应结构与原版一致）+ 解读流水线（输入校验→RAG Mock→LLM→输出后校验→审计），一条调用链贯通 |
| SVC-02 | ✅ 已完成 | 2026-09-03 | POST /api/recommend/explain：curl 验证 200，返回解读 + 免责声明 + 合规结果；无 key 走模板路径（degraded=true），缺参返回 {"error":"缺少推荐结果"}；**2026-09-04 F1**：body 加 mode 参数（analysis 缺省向后兼容 / script 对客参考话术），script 响应文本键为 script、契约字段不变；冒烟补 script 契约 2 项（9/9 全绿） |
| DEP-02 | ✅ 已完成 | 2026-09-03 | key 注入文档化至 CLAUDE.md（DEEPSEEK_API_KEY/BASE_URL/MODEL 环境变量 + Streamlit secrets；key 不入 git；无 key 自动降级）；frontend/.streamlit/secrets.toml 模板已落地（空 key 即降级，填入真实 key 后 start_demo 桥接注入，冒烟第 5 项 source=llm 为生效信号） |
| UI-01 | ✅ 已完成 | 2026-09-03 | frontend/app.py 五区布局（侧边参数配置/一键计算/结果表格/AI 解读面板/常驻免责声明）；浏览器实测两算法均可跑通：KNN 模式出推荐表、关联规则模式出含支持度/置信度的关联推荐表（与后端 curl 结果一致）；AI 解读面板端到端贯通（无 key 显示"基础版解读"降级标注 + 模板五段结构，关联模式含支持度/置信度释义）；解读调用已落审计库（agent_audit_log id=6）；**2026-09-04**：前端业务化 N1-N5（场景语言化入口/人工复核确认/推荐依据摘要/下载出口/适配性软提示）；区 4 双卡（解读卡 + 对客参考话术卡），话术下载仅对客区 + 文件头三重标注 |
| ENG-03 | ✅ 已完成 | 2026-09-03 | git init + .gitignore（*.db / **/secrets.toml / .env / __pycache__ / settings.local.json）；check-ignore 实测四类敏感路径全部命中，git status 敏感项 0 次 |
| ENG-04 | ✅ 已完成 | 2026-09-03 | backend/smoke_test.py：预热 + KNN 相似/异常分流 + Apriori 指标格式 + explain 契约共 7 项检查；实测 7/7 全绿（exit=0），输出与 6.2/6.3 口径一致 |
| DEP-01 | ✅ 已完成 | 2026-09-03 | start_demo.py（secrets.toml 桥接注入后端环境变量 → uvicorn 8000 → 轮询就绪 → streamlit 8501 → 可选 --smoke 自动冒烟；Ctrl+C 一键停双进程）+ start_demo.bat/sh 薄封装；实测双服务就绪、页面可访问、自动冒烟 7/7 全绿、停止后端口释放 |
| SVC-05 | ✅ 已完成 | 2026-09-03 | DEMO_MODE 开关：环境变量控制（True=解读返回预置演示样例、代码路径不触及 llm_client；默认 False 走 LLM）；prompt_templates 提取公共五段模板 + build_demo_explanation；前端 source=demo 显示 info 标注；start_demo.py --demo 参数 + smoke_test.py --expect-demo；验证：默认模式回归 7/7、DEMO 模式 source=demo/degraded=False/五段结构齐全、审计落库 model=demo/is_degraded=0、端到端 --demo --smoke 全绿 |
